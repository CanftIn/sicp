(define size 2)

(begin  (display "size=") 
        (display size) (newline))

(define pi 3.14)
(define radius 10)
(display (* pi (* radius radius)))
(newline)

(define circumference (* 2 pi radius))

(display circumference)
(newline)